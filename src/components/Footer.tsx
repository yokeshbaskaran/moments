const Footer = () => {
  return (
    <div
      className="mt-3 p-3 text-center font-medium border-t border-gray-300"
      style={{
        background: "linear-gradient(to right,#4facfe,#b600fe)",
        color: "white",
      }}
    >
      <span>
        Capture the moments
        <span> {`\u{1F49D}`}</span>
      </span>
    </div>
  );
};

export default Footer;
